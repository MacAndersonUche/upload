# Use public npm registry only (no private packages).
# If Netlify shows "E401 Unable to authenticate", remove any NPM_TOKEN or
# NODE_AUTH_TOKEN from the site's Environment variables in Netlify UI.
registry=https://registry.npmjs.org/
